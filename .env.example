# Root .env.example — Traveloop
# Copy to .env and fill in values. Backend and frontend each have their own .env files too.

# See backend/.env.example and frontend/.env.example for service-specific config.
